{{define "title"}}RHOS: Roster{{end}}
{{define "head"}}{{end}}
{{define "content"}}
<div class="container">
	<div class="row">
		<div class="col-md-11">
			{{NOESCAPE .page}}
		</div>
		<div class="col-md-1">
			<p>Edit</p>
		</div>
	</div>
	<div class="row">
		<div class="col-md-12">
			<p>Last modified: {{.modified}}</p>
		</div>
	</div>
</div>
	{{template "footer" .}}
{{end}}
{{define "foot"}}{{end}}
