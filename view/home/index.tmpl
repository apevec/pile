{{define "title"}}RHOS: Roster{{end}}
{{define "head"}}{{end}}
{{define "content"}}
<div class="container">
	<div class="row">
		<div class="col-md-12">
			{{NOESCAPE .page}}
		</div>
	</div>
	<div class="row">
		<div class="col-md-12">
			<p>
				<a href="/edit" class="btn btn-default" type="button">Edit</a>
			</p>
		</div>
	</div>
	<div class="row">
		<div class="col-md-12">
			<p>Last modified: {{.modified}}</p>
		</div>
	</div>
</div>
{{template "footer" .}}
{{end}}
{{define "foot"}}{{end}}
